import type { WorkflowRun } from "./row-props";

/** Q1 rendering seam; Q3 supplies the workflow-row implementation. */
export function WorkflowRunRow(_props: {
  run: WorkflowRun;
  now: number;
  onOpen?: () => void;
}) {
  return null;
}
