# Packet QP — Write the port packets Q1–Q7 for the own plugin

You write plans only. Do not edit any code, do not run builds, do not commit
in any plugin repo. Output: seven files in
`/home/system/workspaces/LAL/Development/plans/glass-sidebar/`:
`packet-Q1-rows.md`, `packet-Q2-folders.md`, `packet-Q3-live-strip-workflows.md`,
`packet-Q4-project-decor.md`, `packet-Q5-lifecycle.md`,
`packet-Q6-favicons-bulk-sort.md`, `packet-Q7-import-switchover.md`.

## Read

- `00-brief.md` in full; `02-own-plugin-plan.md` in full; `packet-Q0-scaffold.md`.
- As format examples: `packet-B3-folders.md`, `packet-P5d-store-path.md`, `packet-P9-sidebar-icon-menu.md`.
- The fork's source map: run `ls /home/system/workspaces/LAL/Development/forks/bb-sidebar/src` and `git -C /home/system/workspaces/LAL/Development/forks/bb-sidebar log --oneline main..HEAD`; read the first 60 lines of each `src/*.ts(x)` file you assign to a packet (exports and props), and the full test list `ls src/*.test.*`. Do not read `ThreadInbox.tsx` or `server.ts` in full; use `grep -n "^export\|^  async [a-zA-Z]*(" ` to map them.
- From the vendored Project Icons (`/home/system/workspaces/LAL/Development/.bb/vendor/bb-plugins-ariofrio-39a8cf1a/plugins/bb-plugin-project-icons`): `auto-assign.ts`, `matter-classifier.ts`, `project-icon-colors.ts`, `broadcast.ts` first 60 lines each, and `server.ts` lines 1–160 (RPC surface).

## Produce

Each packet, in the brief's packet format (Read / Produce / Verify / commit
subject `[Qn] …`), must:

- Name the exact fork files (and tests) to port, and what to drop (upstream
  scaffolding, `import * as` icon catalog, sibling-store SQLite reads that the
  absorbed decor store replaces, anything the plan's weight budget forbids).
- Keep the brief §4 contract verbatim (attributes, palette, RPC names). New
  RPC names only where the plan introduces new data (project decor,
  lifecycle).
- Q4 must specify: the `project_decor` store as the single source; the
  classifier ported unchanged with its tests; auto colour by the sidebar
  hash rule; icon drawings in `icon-catalog-glyphs.json` generated by a
  script from `@hugeicons/core-free-icons` at build time, read lazily on the
  server, served through RPC `getProjectGlyphs({ projectIds })` and
  `listIconCatalog` for the picker; header chip via
  `experimental_threadHeaderAction`; the "Suggest with AI" item stays
  click-only and optional.
- Q7 must specify: `bb glass-sidebar import` CLI (read-only copy from the
  two old stores, idempotent), the switch-over runbook (install disabled →
  import → disable `bb-sidebar` → enable `glass-sidebar` → verify by eye),
  rollback, README completion, and `forks/check-plugin-slot-conflicts.py`
  as the guard.
- State dependencies between packets exactly as in the plan's table and
  keep each packet's file set disjoint from the others' where possible
  (shared files: `app.tsx`, `server.ts`, `ThreadList.tsx`; list surgical
  edits only).
- Include the weight budget and the "no timers, no new dependencies" line in
  every packet's Verify.

Your final message is a report in the brief's section 5 shape listing the
seven files and, for each, its dependency ids and the fork files it ports.
